# Sprint 18B.8 — Complete Deterministic Chart Composer — Final Report

**Date:** 2026-08-21
**Type:** Real implementation (engine-layer code + tests). No Prisma, no API, no frontend, no AI calls.

## Files created
`tu-vi-chart.ts`, `tu-vi-chart.spec.ts`.

## Complete deterministic chart architecture
`buildTuViChart(input, options): TuViChart` composes all 7 prior phases (18B.1–18B.7) into one immutable, flat structure: `input`, `calendar`, `canChi`, `palaces` (menh/than/layout), `cuc`, `mainStars` (14), `auxiliaryStars` (13), `tuan`, `triet`, `transformations` (4), `versions`. Adds zero new calculation — every field is copied or reshaped from an already-verified upstream context.

## Palace projection
`buildPalaceProjection(chart): PalaceProjection` — 12 entries (one per branch, canonical Tý…Hợi order), each carrying role, Mệnh/Thân markers, main stars, auxiliary stars, Tuần/Triệt markers, and Tứ Hóa transformations present at that palace. A pure derived view: the flat `TuViChart` remains the sole source of truth, confirmed frozen and non-mutating.

## Invariants
`validateTuViChart(chart): void` — throws on the first violation. **Mutation-tested**, not just positive-tested: 11 dedicated tests each construct a deliberately-broken chart (wrong Cục, duplicate star, missing role, mismatched Tuần/Triệt pair, incomplete version bundle, invalid position, etc.) and confirm the specific check actually fires.

## Rule-derived vectors through the complete composer
**VECTOR-B1 (Sprint 18A.5) run fully end-to-end** — Gregorian input `1984-02-02 00:30` (independently confirmed this sprint to be Tết Giáp Tý 1984, the same 1984=Giáp Tý anchor Eastern Horoscope's own table already uses, not generated by the function under test). Every field of the resulting chart — calendar, Can Chi, Mệnh, Thân, Cục, all 14 Chính Tinh, all 11 non-gender-dependent CORE_13 stars, Tuần, Triệt, all 4 Tứ Hóa targets — **matches the hand-derived `golden-vector-v2-spec.md` record exactly**, on the first run, no correction needed. Labeled `RULE_DERIVED_TEST_VECTOR` throughout, never "golden."

## Deterministic repeatability
10-call identity-loop test (`toEqual` deep comparison) plus a frozen-at-every-level check (input/calendar/canChi/palaces/versions all individually `Object.isFrozen`). No `Date.now()`, randomness, DB, network, or AI call anywhere in the composer.

## Version bundle
All 7 identifiers present and asserted to exact values: `rulesetVersion` (`vdttl-1956-v1`), `calendarVersion` (`tuvi-calendar-hnd-v1`), `engineVersion` (`tuvi-engine-v1`, newly wired in this phase), `mainStarVersion` (`tuvi-main-stars-v1`), `auxiliaryVersion` (`core-13-v1`, matching `canonical-ruleset-v1.md` §8's existing identifier), `tuanTrietVersion` (`tuvi-tuan-triet-v1`), `tuHoaVersion` (`tuvi-tu-hoa-v1`).

## Broad invariant coverage
7 additional real-date inputs (spanning the Tết boundary, the leap-month-adjacent lunar-month-change boundary, and both sexes) each produce a fully valid chart under the invariant suite.

## Test results
`tu-vi-chart.spec.ts`: **33/33 pass**, first run, zero bugs found this phase. Cumulative `src/tu-vi`: **319/319 pass** (19 suites, +33 from 18B.7's 286). Eastern Horoscope: **82/82 pass**. Full backend: **144 suites, 1527/1527 tests pass** (+33). Lint/typecheck/`nest build`: all clean.

## Leakage audit
No `@Injectable`/`@Controller`/`@Module` decorator, no Prisma import, no AI/provider/logging call anywhere in `src/tu-vi/`. Prisma schema, frontend, and module registration all unchanged.

## Stop conditions
None triggered.

## Verdict
**PASS — continuing automatically to Sprint 18B.9.**
