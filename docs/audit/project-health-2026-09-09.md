# Mệnh Vi — Project health audit, 2026-09-09

**Verdict: NOT_SAFE_TO_CONTINUE.** The engines, canonical Tarot assets and most unit tests are healthy, but the current repository has a reproducible payment retry defect, an unsafe auth redirect, a failing production build and incorrect Premium state messaging. These must not be mistaken for an environment-only blocker.

This is an audit of the checked-out source and installed dependencies, not a production certification. No production code, test assertion, artwork, configuration, business rule or Git history was changed. Real database/payment-provider/browser verification was unavailable. Evidence distinguishes current uncommitted work (none) from recent committed work and older defects.

1. **CURRENT BRANCH:** `master`, initially clean and up to date with the locally recorded `origin/master`. No remote fetch was performed.

2. **CURRENT HEAD:** `082cbfb0eac55ebe26e9042ab9ebe49c6a2b7f0d` — `[update][commit] update discover`. Previous commit: `53ef3d652c80c2e6c29b3530fa82ecd0cd1c6f1c`. Read the last 25 commits and inspected recent module diffs. The historical premise of many uncommitted passes does not match this checkout.

3. **WORKTREE SUMMARY:** Initially zero staged, modified or untracked files. Existing ignored files include API/web `.env`, API `.env.test`, `.next`, API `dist`, `tsconfig.tsbuildinfo`, dependencies, logs and test-result directories. Existing visual capture/mock helpers were preserved. Audit evidence is under `apps/web/test-results/project-health/`; build/dev logs are under root `test-results/`.

4. **STAGED FILES:** None. No staging, commit, push, reset, clean, checkout or restore was performed.

5. **MODIFIED FILES:** No tracked production or test file was modified by this audit. The final report itself is a new documentation file; generated evidence and caches are ignored.

6. **UNTRACKED FILES:** Initially none. Final `git status --short` contains only `?? docs/audit/project-health-2026-09-09.md`; tracked diff/stat and diff-check are empty/clean. Ten existing ignored visual scripts cover Discover, Natal Chart, Numerology, Premium and Tarot, each with capture/mock variants. Their existence is not evidence that a previous real-stack E2E passed.

7. **CHANGE OWNERSHIP BY MODULE:** Every requested module has **zero current uncommitted changed files**. For useful historical attribution, the two most recent commits contain 55 distinct files: Home 4 production; Tử Vi 9 production + 2 tests; Tarot 5 production + 2 tests; Natal Chart 3 production; Numerology 10 production + 3 tests; Discover 1 production + 1 test; Premium/payment 5 production + 2 tests; shared UI 1 production; E2E 7 tests. Auth/onboarding/settings, shared navigation, visual helpers, backend and shared types: zero files in this two-commit range. Exact paths are recorded in `recent-commit-inventory.json`. UI changes are integrated, but test maintenance is incomplete: the Natal disclosure assertion and Tarot E2E choreography are stale. The shared `AiInterpretation` addition is an optional labels prop with preserved default labels, not an overwrite of another module. No recent engine/API/types changes, conflict markers or accidental file replacement were found. Formatting compression in recent UI files is intentional mixed-content change, not a line-ending-only rewrite.

8. **PARTIAL AUTH PASS STATUS:** **NO NEW WORK** in the current worktree or latest two commits. Auth/settings history points to older commits, including `1eb9105` and `c3760dc`. There is no local partial patch or Auth visual helper establishing an interrupted stopping point. Current screens remain an older implementation with mixed English/Vietnamese copy; this is not proof that a new production pass completed. No Auth production pass was resumed.

9. **ROUTE INVENTORY:** 50 physical `page.tsx` routes, listed in `routes.json`. Required canonical routes all exist: `/`, `/discover`, its five calculator routes, `/reports`, `/premium`, `/premium/return`, `/login`, `/register`, `/verify-email`, `/verify-email/pending`, `/forgot-password`, `/reset-password`, `/onboarding`, `/settings`. They are **VALID source routes**, subject to auth/onboarding redirects and the global build blocker. `/dashboard` is **REDIRECT** to `/`; “Hôm nay” is `/`, not a separate `/today`. Account/profile is `/settings`; standalone `/profile` and `/account` are **MISSING**, with no broken navigation requiring them. Other real pages: `/about`, `/community`, `/contact`, `/privacy`, `/terms`, `/admin`, `/companion`, `/goals`, `/insights`, `/insights/internal`, `/journal`, `/journal/new`, `/journal/archive`, `/journal/[id]`, `/memory`, `/reflections`, `/reviews`, `/reviews/[param]`. Nine `/menh-vi` aliases redirect to canonical routes; the four unmatched legacy subject routes (`su-nghiep`, `suc-khoe`, `tai-chinh`, `tinh-duyen`) are **LEGACY/archived 404**. The legacy layout also calls `notFound()`. Archived prototype components are not canonical production pages. Bundling succeeded, but a complete production build did not pass; source existence is not claimed as authenticated runtime success.

10. **SHARED SHELL HEALTH:** Desktop sidebar, tablet icon rail and phone bottom navigation remain connected to real routes. Premium is reachable through the account menu and conditional sidebar offer; Reports and Eastern Horoscope are reachable from Discover. Home uses its accepted 1536px whole-shell cap, while app pages use the existing 1240px inner content cap. `min-w-0`, mobile safe-area clearance and auth-derived account rendering remain. P2: `pathname.startsWith(item.href)` marks both Discover and a specific discovery module `aria-current="page"` simultaneously. P2: recent Discover and Premium pages nest `<main>` inside AppShell's `<main>`. These are identified without redesign.

11. **HOME STATUS:** Frozen layout/artwork preserved. Real `HomeRoute` remains the entry. Recent changes replaced a fabricated numeric energy score with “đang cập nhật”, adjusted already-accepted layouts and retained real destinations. P2 content debt: the static article cards still advertise article titles/read times but lead to calculator routes rather than article detail pages. There is no CMS or article API behind them. This was not “fixed” by inventing articles or changing the frozen baseline. No fresh visual certification is claimed.

12. **TỬ VI STATUS:** No engine, calendar, lunar, palace or star code changes in the latest two commits or this audit. `/tu-vi/calculate`, `/tu-vi/charts`, detail/history/interpret/archive/restore/delete remain real API calls. Six frontend suites and 25 backend suites are present and included in the unit runs. No visual-QA fixture imported into the production calculator was found.

13. **TAROT STATUS:** Backend draw identity and reversal remain authoritative; the frontend ritual selects/reveals the already-returned reading rather than redrawing cards. The canonical resolver, fallback rendering and card-back separation remain. Six frontend suites and five backend suites are present and passed in the runs. The visual flow is newer than `flow-20` and the Tarot portion of `flow-21`: “Draw”, “Draw again” and the old region do not represent the current multi-step flow. This is a test integration issue, not proof of a broken draw engine.

14. **TAROT 78-CARD INTEGRITY:** **PASS** — 78 fronts: 22 Major, 14 Wands, 14 Cups, 14 Swords, 14 Pentacles. All 78 hashes exactly match `tarot-final-artwork-after-fix-sha256.json`; zero changed, missing or extra canonical fronts. The existing resolver test passed for all 78 paths and the shared card back. No canonical filename, image identity, crop, color or artwork was modified.

15. **NATAL CHART STATUS:** Engine/geocoding code unchanged in recent commits. Birth input still sends the selected opaque `locationToken`, and omits birth time when unknown; no raw-coordinate shortcut was introduced. The wheel uses placements/houses/aspects from the real chart DTO. Collapsible Planets/Houses/Aspects sections remain for smaller screens. Seven backend suites passed. Frontend suite `natal-chart-dashboard.test.tsx` fails at line 116 because it expects `/calculated from your birth data/i`, while the current UI says “Được tính từ dữ liệu sinh của bạn. AI không lựa chọn hoặc thay đổi bất kỳ vị trí nào.” The calculated-vs-AI distinction is still present. **REAL_REGRESSION in test synchronization**, not calculation corruption.

16. **NUMEROLOGY STATUS:** Backend still returns exactly `LIFE_PATH`, `EXPRESSION`, `SOUL_URGE`, `PERSONALITY`, `BIRTHDAY`, `PERSONAL_YEAR`. UI labels are Vietnamese translations of those enums. Master-number reduction remains in the backend authoritative engine. `fullBirthName` and `birthDate` are sent unchanged in shape to `/numerology/calculate`. The guest preview uses expiring session storage and only prefills the birth date; it is neither JWT storage nor an authenticated reading. Three frontend and six backend suites are present and passed. The separate existing guest preview must not be confused with frontend recalculation of the authenticated six-metric result.

17. **DISCOVER STATUS:** Current page uses four real feature destinations, a real Eastern Horoscope entry, three explicitly editorial/non-personalized paths and the existing Reports route. No fabricated CMS, article database or dead article-detail URL was introduced in Discover. It acts as a feature chooser rather than reproducing all Home sections. Its focused page suite passed. Remaining English “Personal Destiny Report” is product naming/copy debt, not an API mismatch.

18. **PREMIUM STATUS:** Backend product is `PREMIUM_30D`. Current local configuration and schema defaults agree on **79,000 VND / 30 days**. Price/currency are read from `/payment/premium-status`; checkout takes no client price. One-time payment, no automatic renewal; repeat backend purchase stacks entitlement duration. Tarot quotas match UI: daily 1/1, single 3/15, three-card 1/10; Free history list is 20, Premium list unbounded with pagination; Premium interpretation enables deeper prompts and consent-aware memory context. Reports generation is backend Premium-gated. P1 state-claim mismatches remain: a historical `PAID` order is displayed as currently active Premium even after entitlement expiry/revocation; Settings treats a failed status lookup as “Free plan”. The cancellation banner also infers “no payment confirmed” from a query parameter. None of these self-grants entitlement, but they are not reliable statements of current backend entitlement/payment state. The hardcoded 30-day copy matches this configuration; changing the duration later would require coordinated UI/DTO work.

19. **PAYMENT SECURITY STATUS:** HMAC verification, signed-data passthrough, amount/currency validation, owner-only order lookup, PENDING-only transition and transaction-scoped grant remain intact. Client redirect parameters do not grant Premium. **P0-01: webhook recovery can permanently drop a valid payment notification.** `payment-webhook.service.ts:71` inserts a durable unique event before `applyPaymentResult`'s transaction. If that transaction fails, the event survives. The identical signed retry hits the unique constraint, `recordEventOrNull` returns null, and the handler acknowledges it without retrying the unprocessed transaction. Its blanket catch at line 191 also treats non-duplicate insert errors as successful duplicate handling. A fault probe using the actual service and actual HMAC provider, with only persistence fault-injected, produced one failed transaction attempt and zero grants after two calls. This is a pre-existing defect; file history predates the recent production passes. Existing happy-path/idempotency unit tests pass but do not cover this failure window. No live money or external checkout was used.

20. **AUTH / SECURITY STATUS:** Web auth remains backend `/auth/me` with HttpOnly access/refresh cookies; no recent localStorage/sessionStorage JWT, frontend-only auth or secret/token logging was found. Mutations use signed double-submit CSRF; Bearer handling and intentional endpoint exceptions are existing behavior, not a new bypass. Password/reset/verification operations remain API-backed with hashed stored tokens. **P1-01: unsafe post-login redirect** in `login-form.tsx:84` and `register-form.tsx:130`: `safeNextPath` accepts a slash followed by a backslash and an external host. Executing each current helper accepts `/\example.invalid`; URL normalization resolves it to `http://example.invalid`. The installed Next router uses `new URL(href, window.location.href)`. Login can therefore leave the site after successful authentication. The Login sink directly uses this accepted path for an already-onboarded user. Register has the same unsafe helper but forwards the value as an encoded onboarding query; its current onboarding destination is fixed, so a direct Register-to-external navigation is not established. Browser exploit execution was not attempted; no token exfiltration is claimed. This helper predates the latest two commits. The new production Auth pass is not complete merely because existing unit tests pass.

21. **ONBOARDING STATUS:** Real `/onboarding` and API-backed conversation remain. Middleware enforces the existing onboarding completion state. One backend conversation-script suite exists; full register/onboard/session progression lives in DB-backed E2E and was not runnable here. No partial new onboarding changes were found. P2: the forwarded `next` query is ignored when onboarding completes, which always navigates through `/dashboard` to `/`. Existing English text remains.

22. **SETTINGS STATUS:** Existing account display, password change, sessions/devices, logout, memory preferences/consent, data export/deletion, notification preferences, Premium and legal links are wired to existing APIs/components. Four settings component suites passed. No separate profile-edit page is implied. Premium fetch failure incorrectly defaults to Free (P1); preferences failure can remain a loading skeleton (P2). Mixed copy/old “Tử Vi Tarot+” naming is pre-existing. Live authenticated behavior is unverified.

23. **REPORTS CURRENT ARCHITECTURE:** `/reports` is an implemented API-backed module, not an empty future route. Required: latest active Natal Chart + Numerology. Optional: up to five recent active Tarot readings from a 90-day window and consent-aware memory snapshot. Server flow: entitlement/readiness checks, AI budget, generation lock, source snapshot, synchronous AI synthesis, schema/grounding validation, persisted READY/FAILED record. Regeneration creates a new report; history/detail are owner-scoped Prisma data. Detail uses `/reports?item=<id>`, not `/reports/[id]`. Readiness and list have skeleton/empty behavior, generation has toast/error behavior, and detail has its own state handling. P2: readiness query errors remain a skeleton; history query errors render “No reports yet”. Two frontend and five backend report suites passed. No Reports production work was started.

24. **TYPECHECK:** `pnpm --filter @beaconvie/web typecheck` **PASS**. Additional `pnpm --filter @beaconvie/api typecheck` **PASS**. No broken imports/types were identified by these checks.

25. **CHANGED-FILE LINT:** Strict current-worktree scope is **NOT APPLICABLE**, since there are no changed files. As an explicit historical check, ESLint ran against all 55 TS/TSX files changed in `HEAD~2..HEAD`: **PASS**, exit 0. Production build's broader required lint step failed at `components/motion/use-prefers-reduced-motion.ts:18` (`react-hooks/set-state-in-effect`). That file last changed in `9d5dbc6`, outside the 55-file scope: **PRE_EXISTING / unrelated to latest two commits**. No rules were disabled and no repository-wide cleanup was attempted.

26. **GIT DIFF CHECK:** `git diff --check` and `git diff --check HEAD~2 HEAD` **PASS**. No staged diff, merge conflict entries or source conflict markers found. No mass line-ending normalization or unrelated tracked edit.

27. **FOCUSED WEB TESTS:** Execution covered all relevant module tests and, unintentionally, the entire web unit inventory: **104 suites, 103 PASS, 1 FAIL, 0 SKIP; 552 tests, 551 PASS, 1 FAIL, 0 SKIP**. Jest 29 did not apply the plural `--testPathPatterns` option supplied to the first run; therefore these are full-web totals, not falsely labeled focused-only totals. The only failure is the stale Natal Chart disclosure assertion described in item 15. No test was weakened, skipped or converted to a snapshot. Expected mocked error paths and test-harness JSX warnings are not production console evidence. Unit suite inventory is in `test-inventory.json`; no separate onboarding web unit suite exists.

28. **CRITICAL BACKEND TESTS:** **152 suites PASS; 1,645 tests PASS; 0 FAIL; 0 SKIP**. The intended pattern contained `tu-vi`, which also matches the absolute repository path `D:/web-tu-vi`, so the run covered the full API unit inventory. Included Tarot/deck/draw, Numerology reductions, Natal/geocoding, Tử Vi engines, payment HMAC/provider/checkout/webhook/entitlement, JWT guards, CSRF, auth registration DTO, onboarding and Reports. Actual auth/session/password/verification lifecycle integration suites are in `apps/api/test/auth.e2e-spec.ts`, `account-security.e2e-spec.ts` and related files; these are **NOT RUN**, not counted as passing or skipped unit tests. They require PostgreSQL/Redis. The separate fault-injection diagnostic exposes a gap despite passing unit tests.

29. **PRODUCTION MOCK LEAK CHECK:** No recent calculator/Discover/Premium runtime import from `test-results` or visual capture fixtures found. Archived `/menh-vi` mock components remain isolated behind redirects/404. Memory evaluation fixtures belong to the existing evaluation runner. AI mock and PayOS mock checkout are intentional development capabilities, rejected by production env validation; webhook signatures remain real in mock checkout mode. Their presence is not evidence of a production leak, nor are mock captures real-stack evidence.

30. **DEV ARTIFACT CHECK:** No accidental `console.log`, `console.debug`, `debugger`, TODO/FIXME or DEV ONLY artifact was found in the recently committed production file scope. Existing CLI evaluation logging and backend operational logs were retained. P3 stale comments remain, e.g. Tarot's “No illustrated artwork exists yet”, outdated NAV_ITEMS commentary and AppShell's old Premium matrix example. Existing test JSX-transform and global-error DOM-container warnings are harness cleanup items.

31. **DATA CONTRACT FINDINGS:** No recent DTO field, enum, calculation API route or history shape mismatch found in reviewed modules; frontend/backend typechecks support this. Confirmed mismatches are semantic: historical paid order versus current entitlement, unavailable Premium status versus Free, obsolete test selectors, and Reports transport error versus empty/no-data presentation. Six numerology enums, natal `locationToken`/optional time, Tarot card identity/reversal and Tử Vi inputs remain aligned.

32. **ENVIRONMENT STATUS:** API/web local env files **SET**. Database/Redis connection settings, JWT/CSRF keys and local mail settings **SET**, but required services **BLOCKING**. PayOS credential fields **SET but recognizable development placeholders / BLOCKING for production**; no sandbox credentials were established. AI key fields are **SET / validity unverified**; no provider request was sent. API `.env` lacks `APP_PUBLIC_URL` (**OPTIONAL locally, BLOCKING in production**). Nominatim geocoding uses default configuration (**OPTIONAL overrides**, live lookup unverified). Sentry/analytics are **OPTIONAL**. Presence of a value does not establish credential validity or production readiness. Secret values were not included in this report or credential inspection output.

33. **DOCKER STATUS:** **ENVIRONMENTAL / BLOCKS_PRODUCTION_ONLY** for the next UI phase by itself. Fresh `docker version` failed with missing `npipe:////./pipe/dockerDesktopLinuxEngine` (“The system cannot find the file specified”). The initial combined ignored-file/Docker command was slow and emitted Windows long-path warnings while enumerating ignored dependencies; its final Docker result confirmed the same missing pipe. No Docker/WSL repair, container deletion or volume operation was attempted. No PostgreSQL/Redis/Mailpit listeners were present at the configured ports when inspected.

34. **REAL STACK STATUS:** **BLOCKED**, not started. Repository Compose requires PostgreSQL host 5433, Redis 6380, Mailpit SMTP 1025/UI 8025; API defaults to 4000 and web to 3000. No invented port or alternate mock API was substituted for the real stack. A web-only dev server was started using the repository script for a limited HTTP check. Production web build bundled successfully, then failed its lint gate; it is not a deployable passing build.

35. **REAL SMOKE TEST STATUS:** Full Home → Discover → calculators → Premium → Auth → Settings flow **NOT RUN** because API/database services are unavailable. A web-only unauthenticated HTTP check returned ten expected 307 redirects to `/login` for protected routes and a 307 from `/dashboard` to `/`. Six public requests initially timed out during cold compilation; after compilation, `/`, `/login`, `/register`, `/verify-email`, `/forgot-password` and `/reset-password` each returned 200 with initial HTML. Evidence is in `http-smoke.json` and `http-smoke-warm.json`. This verifies headers/initial HTML, not full browser hydration or authenticated calculator, payment or settings behavior. The audit-started web server was stopped afterward. Checkout creation/validation, return-state logic and entitlement were inspected and unit-tested only. No external payment or real-money action was initiated.

36. **RESPONSIVE SPOT CHECK:** **NOT RUN / UNVERIFIED** at 390×844, 768×1024, 1536×1200 and 1920×1200. Browser skill/runtime bootstrap returned “No browser is available”; prescribed discovery returned an empty list. No screenshots were reused as fresh evidence and no auth/API mock was presented as real-stack QA. Source-level shell checks and DOM unit tests cannot certify visual layout.

37. **P0 FINDINGS:** **P0-01 payment webhook recovery** (item 19), classified P0 under this audit's payment-integrity definition. A valid signed payment event can be durably marked seen without applying its order/entitlement transaction, and retries then acknowledge without recovery. **BLOCKS_NEXT_PHASE and production** until addressed in a focused payment reliability change. Reproduction is isolated and has no real financial side effects.

38. **P1 FINDINGS:** **P1-01** unsafe Login/Register next redirect; **P1-02** required production build fails on existing reduced-motion lint; **P1-03** Premium return claims active entitlement from historical order status; **P1-04** Settings claims Free when backend status is unavailable. P1-01/02/03/04 are **BLOCKS_NEXT_PHASE** for the requested health gate and also block production approval. They are not attributed to uncommitted changes. The first two predate recent commits; return/status-card logic also existed previously, with stronger localized current-active wording in the latest return UI. No frontend self-grant was found.

39. **P2 FINDINGS:** Stale Natal Chart unit assertion (confirmed failed test); outdated Tarot E2E workflow/selectors in flow-20/21 (static finding, not an executed E2E failure); multiple active nav items; nested main landmarks; Reports/Settings fetch-error presentation; mixed-language/legacy-brand copy; Home article affordances leading to tools. **DOES_NOT_BLOCK_NEXT_PHASE** individually, but test assertions/selectors must be synchronized before using those suites as production evidence. Existing visual helpers require fresh selector review before reuse; they were preserved, not certified.

40. **P3 FINDINGS:** Stale implementation comments, compressed JSX readability and test-harness warning noise. **DOES_NOT_BLOCK_NEXT_PHASE**. No cleanup was performed.

41. **BLOCKS_NEXT_PHASE:** Resolve P0-01 and P1-01 through P1-04 in a narrow reliability/security/build/state-consistency pass. A clean Git worktree and green engine tests do not override these defects. No need to redesign the frozen modules.

42. **BLOCKS_PRODUCTION_ONLY:** Independently restore Docker/services, configure and verify real PayOS credentials, production mail/APP_PUBLIC_URL and chosen AI provider, run DB-backed auth/payment/calculator integration tests, execute the real smoke flow and fresh responsive checks. Docker/browser absence alone would not prevent another UI phase, but the code findings above do.

43. **FIXES MADE DURING AUDIT:** **None to production or existing tests.** Only this report and ignored diagnostic artifacts/logs were added. The fix policy permits only clear P0/P1 regressions from existing uncommitted work; the worktree was clean, and the critical findings are pre-existing committed behavior. P2 test/copy issues were not promoted into an unauthorized cleanup. No artwork change, test weakening, auth pass, Reports pass or business-rule rewrite.

44. **REMAINING ISSUES:** All listed P0/P1/P2 findings remain open. Production build is red; one web test is red; full-stack E2E and visual matrix remain unverified. API unit tests and both typechecks pass. Preserved evidence includes `check-results.json`, `recent-commit-inventory.json`, `routes.json`, `test-inventory.json`, `payment-retry-probe.cjs`, `auth-redirect-probe.cjs`, `recent-committed-lint.log`, `http-smoke.json` and `http-smoke-warm.json` under `apps/web/test-results/project-health/`, plus `test-results/project-health-build.log` and `project-health-web.log`. The payment probe can be run from `apps/api` using `node -r ts-node/register/transpile-only ../web/test-results/project-health/payment-retry-probe.cjs`; it uses synthetic keys and no DB/network.

45. **RECOMMENDED NEXT PHASE:** A bounded health-gate remediation for the documented payment retry, redirect validation, build and Premium-state issues, followed by relevant regression tests. After those gates pass, select the intended Auth/Onboarding/Settings production pass or Reports experience pass from this verified architecture. Do not start either full production pass as part of this audit.

46. **FINAL VERDICT:** **NOT_SAFE_TO_CONTINUE.** This conclusion is based on reproducible code defects and the failed build, not merely unavailable Docker. The complete visual/real-stack certification remains outstanding, and passing unit totals are reported without implying those missing checks passed.
